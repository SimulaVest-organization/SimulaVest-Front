import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SimulaVest",
  description: "Prepare-se para os principais vestibulares com simulados, questões e estatísticas personalizadas.",
  icons: {
    icon: "/Logo_semfundo.png",
    shortcut: "/Logo_semfundo.png",
  },
  keywords: 'vestibular, ENEM, simulado, preparação, educação',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
